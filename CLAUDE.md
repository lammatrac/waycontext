# Project Notes

## Code Context MCP

This project is indexed by the `code-context` MCP server under the project
name **`waycontext`**. When using `code-context` tools
(`project_overview`, `search_code`, `get_graph`, `get_callers`, `get_symbol`,
`index_project`, etc.) for this repo, use/target project `waycontext`.
